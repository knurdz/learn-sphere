{
  # CADDY_EMAIL must be set in /opt/learnsphere/env/compose.env (loaded via env_file).
  email {$CADDY_EMAIL}
}

learnsphere.knurdz.org {
  encode gzip

  reverse_proxy api:3000 {
    transport http {
      read_timeout 300s
      write_timeout 300s
    }
  }
}
